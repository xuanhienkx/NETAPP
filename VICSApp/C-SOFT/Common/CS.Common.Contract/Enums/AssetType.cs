namespace CS.Common.Contract.Enums
{
    public enum AssetType : byte
    {
        Money = 0,
        Stock = 1,
        Asset = 3
    }

    public enum SourceType : byte
    {
        Normal
    }
}